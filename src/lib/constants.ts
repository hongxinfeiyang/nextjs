// ============================================================================
// 应用常量定义模块
// ============================================================================
// 集中管理应用中使用的各种静态常量，包括商品分类、订单状态映射、
// 分页大小等。使用 as const 确保类型安全，避免魔法字符串分散在代码各处。
// ============================================================================

/**
 * 商品分类列表
 *
 * 使用 const 断言确保数组元素类型为字面量联合类型，
 * 可用于下拉选择框渲染和类型约束。
 */
export const CATEGORIES = ['电子产品', '服装', '家居', '运动', '图书'] as const;

/**
 * 订单状态中文映射
 *
 * key 为数据库中存储的英文状态码，value 为前端展示的中文标签。
 * 使用 as const 确保键值对的类型不可变。
 */
export const ORDER_STATUSES = {
  PENDING: '待处理',     // 订单已创建，等待支付或处理
  PAID: '已支付',        // 已完成支付，等待发货
  PROCESSING: '处理中',  // 订单正在处理中
  SHIPPED: '已发货',     // 商品已发出，正在运输途中
  DELIVERED: '已送达',   // 商品已送达客户手中
  CANCELLED: '已取消',   // 订单已被取消
} as const;

/**
 * 默认分页大小
 *
 * 用于商品列表、订单列表等需要分页的场景，
 * 每页显示 12 条记录。
 */
export const PAGE_SIZE = 12;
